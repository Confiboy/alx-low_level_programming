#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	char love;

	for (love = 'a'; love <= 'z';
		putchar(love);
	for (love = 'A'; love <= 'Z';
		putchar(love);
	putchar('\n');
	return (0);
}
